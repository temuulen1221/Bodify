// Use expo-router's auto-registered entry for native and web.
// This ensures the app starts from files in the `app/` directory
// (e.g., `_layout.tsx`, `index.tsx`) instead of the legacy `app.js`.
import 'expo-router/entry';
